'use strict';

let create = {
  handler: (request, reply) => {
    reply({});
  }
};

let getAll = {
  handler: (request, reply) => {
    reply({});
  }
};

let getById = {
  handler: (request, reply) => {
    reply({});
  }
};

let destroy = {
  handler: (request, reply) => {
    reply({});
  }
};

module.exports = {
  create: create,
  getAll: getAll,
  getById: getById,
  destroy: destroy
};